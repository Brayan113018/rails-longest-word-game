<h1>New Game</h1>

<div class="letters">
   <% @letters.each do |letter| %>
    <%= letter.upcase %>
    <% end %>
</div>

<br><p>What is the longest work you can find?</p></br>
<form action="/score" method="post" data-turbo="false">
  <!-- ... -->
  <div class="input-group mb-3">
   <input type="text" name="word" placeholder="Who are you looking for?" value="<%= params[:word]%>" >
  <input type="submit">
</div>
</form>

<%= hidden_field_tag :authenticity_token, form_authenticity_token %>
